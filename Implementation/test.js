/*var AnyFile = require('any-file');
var af = new AnyFile();
var serv = servers["floe"];
var fromFile = serv.protocol+"://"+serv.username+":"+serv.password+"@"+serv.address+serv.logs.apache.access.path+serv.logs.apache.access.file;
console.log("Running: " + fromFile);
var toFile = "test";

af.from(fromFile).to(toFile, function(err, res) {
	if(res) {
		console.log(res)
	} else {
		console.log(err)
	}
});*/